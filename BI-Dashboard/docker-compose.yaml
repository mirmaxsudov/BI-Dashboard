version: "3.8"
services:
  java_app:
    build:
      context: ./
    container_name: test-app
    ports:
      - "8080:8080"
    networks:
      - my-net
    restart: always
networks:
  my-net:
volumes:
  my-vol: